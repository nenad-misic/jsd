// Autogenerated from {{model_filename}} file
// Created at {{timestamp|strftime}}

var base_{{entity.name|lower}}_repository = require('../autogenerated/repositories/base_{{entity.name|lower}}_repository')

var repository = {
    create: (entity) => {
        return base_{{entity.name|lower}}_repository.create(entity);
    },
    update: (entity) => {
        return base_{{entity.name|lower}}_repository.update(entity);
    },
    delete: (id) => {
        return base_{{entity.name|lower}}_repository.delete(id);
    },
    exists: (id) => {
        return base_{{entity.name|lower}}_repository.exists(id);
    },
    count: () => {
        return base_{{entity.name|lower}}_repository.count();
    },
    findById: (id) => {
        return base_{{entity.name|lower}}_repository.findById(id);
    },
    getAll: () => {
        return base_{{entity.name|lower}}_repository.getAll();
    }
}

module.exports = repository;