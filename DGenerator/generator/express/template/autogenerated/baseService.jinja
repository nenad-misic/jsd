// Autogenerated from {{model_filename}} file
// Created at {{timestamp|strftime}}

var repositories = require('../../repositories')

var service = {
    create: (entity) => {
        // Validates entity and passes it to repository if valid. Throws exception if not valid >>> throw new Error('Error description');
        // TODO - validation

        return "created {{entity.name|lower}}";
    },
    update: (entity) => {
        // Validates entity and passes it to repository if valid. Throws exception if not valid >>> throw new Error('Error description');
        // TODO - validation
        if (!entity.id)
            throw new Error('ID cannot be null or undefined')

        return "updated {{entity.name|lower}}"
    },
    delete: (id) => {
        // Validates id and passes it to repository if valid. Throws exception if not valid >>> throw new Error('Error description');
        // TODO - validation
        if (!id)
            throw new Error('ID cannot be null or undefined');

        return "deleted {{entity.name|lower}}"
    },
    exists: (id) => {
        // Validates id and passes it to repository if valid. Throws exception if not valid >>> throw new Error('Error description');
        // TODO - validation
        if (!id)
            throw new Error('ID cannot be null or undefined');

        return "exists {{entity.name|lower}}"
    },
    count: () => {
        // Delegates to repository.
        return "count {{entity.name|lower}}"
    },
    findById: (id) => {
        // Validates id and passes it to repository if valid. Throws exception if not valid >>> throw new Error('Error description');
        // TODO - validation
        if (!id)
            throw new Error('ID cannot be null or undefined');

        return "found by ID {{entity.name|lower}}"
    },
    getAll: () => {
        // Delegates to repository.

        return "get all {{entity.name|lower}}"
    },
}

module.exports = service;
