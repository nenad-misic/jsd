import {api} from '../Environment/environment';
import base_{{entity.name|lowercase}}Service from './autogenerated/base_{{entity.name|lowercase}}Service'

import axios from 'axios';

let {{entity.name|lowercase}}Service = {
    create: (entity) => {
        return base_{{entity.name|lowercase}}Service.create(entity);
    },
    update: (entity) => {
        return base_{{entity.name|lowercase}}Service.update(entity);
    },
    delete: (id) => {
        return base_{{entity.name|lowercase}}Service.delete(id);
    },
    findById: (id) => {
        return base_{{entity.name|lowercase}}Service.findById(id);
    },
    getAll: () => {
        return base_{{entity.name|lowercase}}Service.getAll();
    }, 
    exists: (id) => {
        return base_{{entity.name|lowercase}}Service.exists(id);
    }, 
    count: () => {
        return base_{{entity.name|lowercase}}Service.count();
    }
};

export default {{entity.name|lowercase}}Service;
